#-------------------------------------------------------------------------------#
#            Discover all capabilities of Qodana in our documentation           #
#             https://www.jetbrains.com/help/qodana/about-qodana.html           #
#-------------------------------------------------------------------------------#

name: Qodana (disabled)

# Qodana workflow disabled per request. This workflow will only run manually (workflow_dispatch)
on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: |
          echo "Qodana workflow disabled — file retained for history. To remove, delete this file."
